Prestoliv GTM — one-shot import (GTM-W5G7H4GP)
================================================

WHAT YOU GET (auto-generated)
  • 30 Data Layer variables (page_path, lead_source, etc.)
  • 9 Custom Event triggers (virtual_page_view, generate_lead, etc.)
  • 10 GA4 tags wired to G-Z21L9R9W4V

STEP 1 — Import into Google Tag Manager
  1. Open https://tagmanager.google.com → container GTM-W5G7H4GP
  2. Admin (gear icon) → Import Container
  3. Choose file: gtm/prestoliv-analytics-import.json
  4. Choose workspace: Default Workspace
  5. Import option: Merge (recommended) or Overwrite if container is empty
  6. Confirm → Submit → Publish

STEP 2 — Vercel env vars (Production)
  Keep:
    VITE_GTM_ID=GTM-W5G7H4GP
    VITE_CLARITY_PROJECT_ID=wz5rob2h5u

  Remove (GA4 now runs inside GTM):
    VITE_GA_MEASUREMENT_ID

  Redeploy after changing env vars.

STEP 3 — Test
  1. GTM → Preview → enter https://www.prestoliv.com
  2. Browse pages → Tags Fired should show "GA4 - page_view"
  3. Open consultation modal → "GA4 - All Other Custom Events"
  4. GA4 → Reports → Realtime → confirm active user

Regenerate import file after event changes:
  npm run gtm:generate
